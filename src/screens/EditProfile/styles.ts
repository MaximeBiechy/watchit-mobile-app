import { StyleSheet } from 'react-native';
import { PADDING_VERTICAL } from '../../styles/responsives.ts';

const styles = StyleSheet.create({
  container: {
    marginTop: PADDING_VERTICAL * 4,
  },
});

export default styles;
